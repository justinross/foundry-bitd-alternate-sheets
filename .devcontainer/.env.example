# Foundry VTT Configuration
# Copy this file to .env and fill in your values
# DO NOT commit your actual credentials to version control

# Foundry VTT Credentials
FOUNDRY_USERNAME=your-foundry-username
FOUNDRY_PASSWORD=your-foundry-password
FOUNDRY_ADMIN_KEY=your-admin-key

# Foundry Version (release, 12, 13, etc.)
# Options: release (latest stable), or specific version like 12, 13, etc.
FOUNDRY_VERSION=release

# External Port Configuration
# Recommended convention to avoid conflicts when running multiple Foundry versions:
#   - 30000 for 'release' (latest stable)
#   - 30012 for version 12
#   - 30013 for version 13
#   - etc.
# IMPORTANT: If you change this, also update the forwardPorts array in .devcontainer/devcontainer.json
FOUNDRY_EXTERNAL_PORT=30000

# Module Name (used for workspace and module mount paths)
# Change this when using the devcontainer for different Foundry module projects
MODULE_NAME=your-module-name
